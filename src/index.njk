{% extends 'layouts/default.njk' %}

{% set title = 'Diary' %}
{% set bodyClass = "diaryEntry" %}

{% block content %}
{# {{ events | dump | safe }} #}
{# {{ diary | dump | safe }} #}
{# {{ collections.entriesByMonth | log }}
{{ collections.entriesByYear | log }} #}

<section class="diary">
<h1>🖼️ Diary</h1>
<div class="calendar"></div>
<a class="diary-link" href="{{ diary | lastImage }}"><img class="diary-img" data-index="{{diary.length - 1}}" src="{{ diary | lastImage }}" /></a>
<nav class="diary-actions">
  <button data-action="previous">
    <svg fill="currentColor" viewBox="0 0 20 20" class="w-8 h-8"><path fill-rule="evenodd" d="M12.707 5.293a1 1 0 010 1.414L9.414 10l3.293 3.293a1 1 0 01-1.414 1.414l-4-4a1 1 0 010-1.414l4-4a1 1 0 011.414 0z" clip-rule="evenodd"></path></svg>
    <span>Previous</span>
  </button>
  <button data-action="calendar">
    <svg fill="currentColor" viewBox="0 0 20 20" class="w-8 h-8"><path fill-rule="evenodd" d="M6 2a1 1 0 00-1 1v1H4a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-1V3a1 1 0 10-2 0v1H7V3a1 1 0 00-1-1zm0 5a1 1 0 000 2h8a1 1 0 100-2H6z" clip-rule="evenodd"></path></svg>
    <span>Calendar</span>
  </button>
  <button data-action="latest">
    <svg fill="currentColor" viewBox="0 0 20 20" class="w-8 h-8"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"></path></svg>
    <span>Latest</span>
  </button>
  <button data-action="next">
    <svg fill="currentColor" viewBox="0 0 20 20" class="w-8 h-8"><path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"></path></svg>
    <span>Next</span>
  </button>
</nav>
</section>

<section class="events">
<h1>📆 Events</h1>
{% for event in events %}
  <a href="{{event.link}}" class="event">
    <h2>{{event.emoji}} {{event.name}}</h1>
    <p>{{event.date}}</p>
    <p>{{event.outcome}}</p>
  </a>
{% endfor %}

</section>


{% endblock %}

{% set js %}
<script>

const diary = {{ diary | dump | safe}}

document.querySelector('button[data-action="previous"]').addEventListener('click', e => {
  let image = document.querySelector('.diary-img')
  let currentIndex = parseInt(image.dataset.index)
  if(!currentIndex) currentIndex = diary.findIndex(entry => entry.date.full === image.src.split('diary/')[1].split('.jpg')[0])
  let index = currentIndex - 1
  if(index === -1) index = diary.length - 1;
  changeImg(diary[index].image, index)
})

document.querySelector('button[data-action="next"]').addEventListener('click', e => {
  let image = document.querySelector('.diary-img')
  let currentIndex = parseInt(image.dataset.index)
  if(!currentIndex) currentIndex = diary.findIndex(entry => entry.date.full === image.src.split('diary/')[1].split('.jpg')[0])
  let index = currentIndex + 1
  if(index === diary.length) index = 0;
  changeImg(diary[index].image, index)
})

document.querySelector('button[data-action="latest"]').addEventListener('click', e => {
  changeImg(diary[diary.length - 1].image, diary.length - 1)
})

document.querySelector('button[data-action="calendar"]').addEventListener('click', e => {
  document.querySelector('section.diary .calendar').classList.toggle('visible')
})

console.log('hello')
const cal = jsCalendar.new(".calendar", "now", { "monthFormat": "month YYYY" });
cal.select({{ diary | calendarDates | dump | safe }})
cal.onDateClick((e, calDate) => {
  if(!e.target.classList.contains('jsCalendar-selected')) return
  console.log(e, calDate)
  const day = `0${calDate.getDate()}`.slice(-2)
  const month = `0${calDate.getMonth()+1}`.slice(-2)
  const imgURL = `assets/images/diary/${calDate.getFullYear()}-${month}-${day}.jpg`
  document.querySelector('section.diary .calendar').classList.remove('visible')
  changeImg(imgURL)
})

function changeImg(imgURL, index = "") {
  document.querySelector('.diary-img').src = imgURL
  document.querySelector('.diary-img').dataset.index = index
  document.querySelector('.diary-link').href = imgURL
}

</script>
{% endset %}